import { Heading } from '@chakra-ui/react';

const Header = () => {
  return (
    <header>
      <Heading as="h1" size="3xl">
        Password Vault
      </Heading>
    </header>
  );
};

export default Header;
